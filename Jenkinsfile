pipeline {
    agent { label "maquina1"}

    stages {
        stage('Paso 1: Crear ficheros') {
            steps {
                sh 'cd ~'
                sh 'touch muahahaha.txt'
                sh 'touch otroxD.sh'
            }
        }
    }
}